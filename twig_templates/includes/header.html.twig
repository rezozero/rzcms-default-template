<!DOCTYPE html>
<!--[if lt IE 7]><html xmlns="http://www.w3.org/1999/xhtml" class="no-js lt-ie9 lt-ie8 lt-ie7"><![endif]-->
<!--[if IE 7]><html xmlns="http://www.w3.org/1999/xhtml" class="no-js lt-ie9 lt-ie8"><![endif]-->
<!--[if IE 8]><html xmlns="http://www.w3.org/1999/xhtml" class="no-js lt-ie9"><![endif]-->
<!--[if gt IE 8]><!-->
<html xmlns="http://www.w3.org/1999/xhtml" 
    xmlns:fb="https://www.facebook.com/2008/fbml" 
    xml:lang="{{ head.lang }}" 
    lang="{{ head.lang }}" 
    dir="{{ head.direction }}"><!--<![endif]-->
    <head>
        <meta charset="UTF-8" />
        <title>{{ head.title }}</title>
        <meta name="generator" content="RZ-CMS {{ head.cmsVersion }}" />
        <meta name="description" content="{{ head.description }}" />
        <meta name="keywords" content="{{ head.keywords }}" />
        {# 
        -------- Favicons and Apple favicon -----------
        #}
        <link rel="shortcut icon" href="{{ head.imgFolder }}favicon.ico" />
        <link rel="apple-touch-icon-precomposed" href="{{ head.imgFolder }}apple-icon.png"/>
        <meta name="apple-mobile-web-app-status-bar-style" content="black" />
        <meta name="apple-mobile-web-app-capable" content="no" />
        {# 
        -------- Import styles with LESS or CSS -----------
        #}
        {% if head.useLESS == true %}
            <link rel="stylesheet/less" type="text/css" href="{{ head.cssFolder }}style.less">
        {% else %}
            <link rel="stylesheet" href="{{ head.cssFolder }}style.css" type="text/css" />
       {% endif %}
       {{ head.webFonts|raw }}
       {{ head.cssLibs|raw }}
       {# 
        ------------ Import your own custom modernizer to keep good performances ------------
        #}
        {% if head.useCDN %}
        <script src="//cdnjs.cloudflare.com/ajax/libs/modernizr/2.7.1/modernizr.min.js" type="text/javascript"></script>
        {% else %}
        <script type="text/javascript" src="{{ head.jsFolder }}vendor/modernizr-2.7.1.min.js"></script>
        {% endif %}
    </head>